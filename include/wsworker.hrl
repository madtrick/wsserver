%-include_lib("wsock/include/wsock.hrl").

%-define(HANDSHAKE, handshake).
%-define(OPEN, open).
%-define(CLOSE, close).

%-record(handler,{
%    module,
%    state
%  }).

%-record(state, {
%    handler,
%    status = ?HANDSHAKE,
%    fragmented_message
%  }).
